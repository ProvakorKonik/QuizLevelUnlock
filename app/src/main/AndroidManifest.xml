<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bakhtiar.quizmate">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizMate">
        <activity android:name=".View.Rank"></activity>
        <activity android:name=".View.QuestionAdd" />
        <activity android:name=".View.QuestionsList" />
        <activity android:name=".View.LevelAdd" />
        <activity android:name=".View.Level" />
        <activity android:name=".View.SubjectAdd" />
        <activity android:name=".LoginRegistration" />
        <activity android:name=".View.MainActivity" />
        <activity android:name=".LoginStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>